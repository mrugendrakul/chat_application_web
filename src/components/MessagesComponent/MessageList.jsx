import React from 'react'
import Message from '../../dataLayer/Message'

const MessageList = ({
    message = Message(),
    currentUser
}) => {
  return (
    <div>MessageList</div>
  )
}

export default MessageList